replicaCount: 1

image:
  repository: ghcr.io/yam1x/diploma-orchestrator-ui
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  enabled: true
  className: nginx
  host: {{ .Values.ingress.host | quote }}
