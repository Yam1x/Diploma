FROM node:22-alpine AS build

WORKDIR /app
COPY apps/orchestrator-ui/package.json /app/package.json
RUN npm install
COPY apps/orchestrator-ui /app
RUN npm run build

FROM nginx:1.27-alpine
COPY apps/orchestrator-ui/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
