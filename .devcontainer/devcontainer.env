# Reserved for local environment variables consumed by the devcontainer runtime.
# Base shared config lives in .devcontainer/devcontainer.base.json.
# Local active config lives in .devcontainer/devcontainer.json and is gitignored.
