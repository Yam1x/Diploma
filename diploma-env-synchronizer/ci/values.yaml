image:
  registry: ghcr.io
  repository: yam1x/diploma-env-synchronizer
  tag: latest
  pullPolicy: Always

resources:
  limits:
    # WARNING: small value of cpu limit may cause failing during applying helmfile
    cpu: 500m
  requests:
    cpu: 1m
    memory: 256Mi

extraConfigMapEnvVars:
